# S03_TodoWrite.py 调用流程

## 用户请求
```
Refactor the file hello.py: add type hints, docstrings, and a main guard
```

## 执行流程

### Round 1: 读取文件
```
LLM → read_file("hello.py") → 文件为空
```

### Round 2: 尝试列出目录（失败）
```
LLM → bash("ls -la /Users/.../Learn Claude Code/") → 失败
原因：路径包含空格，未加引号
```

### Round 3: 修正后列出目录（成功）
```
LLM → bash("ls -la \"/Users/.../Learn Claude Code/\"") → 成功
发现 hello.py 存在但为空
```

### Round 4: 触发提醒 → 创建 Todo 列表
```
系统 → <reminder>Update your todos.</reminder>

LLM → todo([
  {"id": "1", "text": "Create hello.py with type hints", "status": "in_progress"},
  {"id": "2", "text": "Add docstrings", "status": "pending"},
  {"id": "3", "text": "Add main guard", "status": "pending"}
])

显示：
[>] #1: Create hello.py with type hints
[ ] #2: Add docstrings
[ ] #3: Add main guard
(0/3 completed)
```

### Round 5: 写入文件
```
LLM → write_file("hello.py", content) → Wrote 446 bytes
```

### Round 6: 更新 Todo 为完成
```
LLM → todo([
  {"id": "1", "status": "completed"},
  {"id": "2", "status": "completed"},
  {"id": "3", "status": "completed"}
])

显示：
[x] #1: Create hello.py with type hints
[x] #2: Add docstrings
[x] #3: Add main guard
(3/3 completed)
```

### Round 7: 读取验证
```
LLM → read_file("hello.py") → 返回完整代码
```

### Round 8: 输出最终结果
```
LLM 直接回答：Refactoring completed successfully...
包含完整的重构后代码
```

## 关键观察

| 机制 | 作用 |
|------|------|
| 3轮提醒 | LLM 3轮未用 todo，系统自动插入提醒 |
| 任务追踪 | LLM 主动创建、更新、完成任务 |
| 自我纠错 | LLM 发现路径错误后自动加引号重试 |

## 生成的 hello.py

```python
"""A simple hello world module with greeting functionality."""


def greet(name: str) -> str:
    """Generate a greeting message for a given name."""
    return f"Hello, {name}!"


def main() -> None:
    """Main function to run when module is executed directly."""
    print(greet("World"))


if __name__ == "__main__":
    main()
```
